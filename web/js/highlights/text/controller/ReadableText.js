/**
 * Take the physical layout of the on screen PDF text and build a readable
 * paragraph using the element positions.  Elements that are visually contiguous
 * and close together are almost certainly part of the same contiguous line of
 * text.
 */
class ReadableText {

    static toText(textSelections) {

    }

}
