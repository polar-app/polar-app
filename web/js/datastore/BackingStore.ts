/**
 * A backing store allows us to isolate storage of various data types.  For
 * example, we could store video on a video hosting service or images on a
 * specific service.
 */
export enum BackingStore {

    VIDEO,
    IMAGE

}
