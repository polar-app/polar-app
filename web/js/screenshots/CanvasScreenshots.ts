/**
 * Performs screenshots using the underlying canvas of the document we're
 * working with.  For PDFs we use the native Canvas and for PHZ we can use
 * html2canvas.
 */
export class CanvasScreenshots {


}
