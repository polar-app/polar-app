{
  "name": "polar-web-extension-api",
  "version": "0.0.0",
  "description": "Provides just the basic API that we can use in both the frontend and backend for API requests and responses.",
  "main": "index.js",
  "scripts": {
    "_test": "mocha --timeout 20000 --exit 'src/**/*Test.js'",
    "test": "RESULT=\"$(find . -name '**Test.js' -o -name '**TestN.js' -o -name '**TestNK.js' -not -path 'node_modules/*')\" && if [ -z \"$RESULT\" ]; then echo 'No tests'; else yarn run mocha; fi;",
    "compile": "RESULT=\"$(find . -name '*.ts' -o -name '*.tsx' -not -path './node_modules/*' -not -name '*.d.ts*')\" && if [ -z \"$RESULT\" ]; then echo 'Nothing to Compile'; else yarn run tsc; fi;",
    "build": "webpack --config webpack.config.js",
    "mocha": "mocha -p --jobs=1 --timeout 60000 --exit './{,!(node_modules)/**}/*Test.js' './{,!(node_modules)/**}/*TestN.js' './{,!(node_modules)/**}/*TestNK.js'",
    "eslint": "eslint -c ./.eslintrc.json .",
    "eslintfix": "eslint -c ./.eslintrc.json . --fix",
    "tsc": "tsc",
    "karma": "RESULT=\"$(find . -name '**Test.js' -o -name '**TestK.js' -o -name '**TestNK.js' -not -path 'node_modules/*')\" && if [ -z \"$RESULT\" ]; then echo 'No tests'; else npx karma start; fi;"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "4.2.11",
    "@types/chrome": "0.0.114",
    "@types/mocha": "=9.0.0",
    "chai": "4.2.0",
    "mocha": "=9.1.1",
    "polar-eslint": "0.0.0",
    "polar-karma": "0.0.0",
    "polar-typescript": "0.0.0",
    "polar-webpack": "0.0.0",
    "ts-loader": "=8.0.18",
    "webpack": "=5.26.3",
    "webpack-cli": "=4.5.0"
  },
  "dependencies": {
    "@types/karma": "^5.0.1",
    "@types/node": "=14.14.35",
    "polar-shared": "0.0.0"
  },
  "files": [
    "**/*.js",
    "**/*.d.ts",
    "package.json"
  ]
}
