{
  "name": "polar-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "lerna run compile",
    "publish": "lerna exec --no-private -- npm publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "lerna run compile && lerna run dist && lerna && lerna exec --no-private -- npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
