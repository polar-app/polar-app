{
  "name": "polar-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "lerna run compile",
    "publish": "lerna exec --no-private -- npm publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist-prepare": "./sbin/mgit clean -f && ./sbin/mgit reset --hard HEAD && ./sbin/mgit pull",
    "dist-release": "lerna bootstrap && lerna run compile && lerna run dist && lerna exec --no-private -- npm publish && lerna run dist-release"
  },
  "deprecated": {
    "_dist_version_comment":  "this didn't work because npm version patch doesn't update dependency versions nor does it keep them globally in sync.",
    "_dist-version": "lerna exec npm version patch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
